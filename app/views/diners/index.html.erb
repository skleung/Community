<h1>Listing diners</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @diners.each do |diner| %>
      <tr>
        <td><%= diner.name %></td>
        <td><%= link_to 'Show', diner %></td>
        <td><%= link_to 'Edit', edit_diner_path(diner) %></td>
        <td><%= link_to 'Destroy', diner, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Diner', new_diner_path %>
