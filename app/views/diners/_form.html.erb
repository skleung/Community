<%= form_for(@diner) do |f| %>
  <% if @diner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@diner.errors.count, "error") %> prohibited this diner from being saved:</h2>

      <ul>
      <% @diner.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
